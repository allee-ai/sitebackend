# ─── Application ──────────────────────────────────────────────────────────────

# PostgreSQL connection string (Railway sets this automatically)
DATABASE_URL="postgresql://user:password@localhost:5432/sitebackend"

# Stripe
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Frontend URL for Stripe redirect (no trailing slash)
FRONTEND_URL="https://allee-ai.com"

# Comma-separated list of origins allowed by CORS
ALLOWED_ORIGINS="https://allee-ai.com"

# Admin API key – required to create/update products
ADMIN_API_KEY="change-me-before-production"

# ─── askAI (future) ───────────────────────────────────────────────────────────
# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# AI_MODEL=gpt-4o
# AI_MAX_TOKENS=1024

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
